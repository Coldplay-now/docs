---
title: "FAQ"
description: "常见问题与排障入口"
---

## 为什么我看不到 Key 明文？

这是安全设计的一部分。默认不长期展示明文，防止误泄漏。

## CLI 是否会把 Key 打到日志里？

推荐使用 `apikey run` 注入环境变量，该模式不会把明文直接写在命令参数里。

## 如何迁移到新 Mac？

先导出元数据，再在新设备导入并重新校验关键供应商连通性。
