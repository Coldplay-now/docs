---
title: "导入导出"
description: "通过 .env / JSON 在项目之间迁移密钥元数据"
---

## 导入

支持从 `.env` 批量导入，并自动识别常见供应商字段。

导入前建议：

- 先在测试环境试导入
- 检查变量命名是否冲突
- 对高权限 Key 设置更短自动锁定时间

## 导出

可导出为 `.env` 或 JSON 用于迁移。  
导出后请确保：

- 文件不进入 Git 仓库
- 仅在可信设备保存
- 完成后及时删除临时文件
